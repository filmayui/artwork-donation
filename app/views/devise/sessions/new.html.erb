<%= render "shared/second-header" %>

<div id="background-login-image">

  <div class="artist-contents">
    <div class="svgSample1">
    <svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%">
      <defs>
        <text dy="0.4em" id="outTextx">ユーザーログイン</text>
      </defs>
      <use x="50%" y="50%" xlink:href="#outTextx"></use>
      <use x="50%" y="50%" xlink:href="#outTextx"></use>
    </svg>
    </div>

    ログイン情報を入力してください。

    <%= form_with model: @user, url: user_session_path, class: 'registration-main', local: true do |f| %>

    <%= render 'shared/error_messages', model: @user %>

    <div class='login-flash-message'>
      <%= flash[:notice] %>
      <%= flash[:alert] %>
    </div>

    <div class="form-group">
      <%= f.label :email, "メールアドレス", class:"label" %><br>
      <%= f.email_field :email, class:"email-textarea", id:"email", placeholder:"PC・携帯どちらでも可", autofocus: true, maxlength:"20" %>
    </div>
    <div class="form-group">
      <%= f.label :password, "パスワード", class:"label" %><br>
      <%= f.password_field :password, class:"password-textarea", id:"password", maxlength:"8" %>
    </div>
    <div class='login-btn'>
      <%= f.submit "ログイン" ,class:"submit-btn" %>
    </div>
  </div>
  <% end %>

</div>

<%= render "shared/footer" %>