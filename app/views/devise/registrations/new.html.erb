<%= render "shared/second-header" %>

<div id="background-registration-image">

  <div class="artist-contents">
    <div class="svgSample1">
    <svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%">
      <defs>
        <text dy="0.4em" id="outTextx">ユーザー情報登録</text>
      </defs>
      <use x="50%" y="50%" xlink:href="#outTextx"></use>
      <use x="50%" y="50%" xlink:href="#outTextx"></use>
    </svg>
    </div>

    ユーザー情報を入力してください。

    <%= form_with model: @user, url: user_registration_path, class: 'registration-main', local: true do |f| %>

    <%= render 'shared/error_messages', model: @user %>

    <div class="form-group">
      <%= f.label :nickname, "ニックネーム", class:"label" %><br>
      <%= f.text_area :nickname, class:"nickname-textarea", id:"nickname", placeholder:"例) j-speed", maxlength:"7" %>
    </div>

    <div class="form-group">
      <%= f.label :email, "メールアドレス", class:"label" %><br>
      <%= f.email_field :email, class:"email-textarea", id:"email", placeholder:"PC・携帯どちらでも可", autofocus: true, maxlength:"20" %>
    </div>

    <div class="form-group">
      <%= f.label :password, "パスワード", class:"label" %><br>
      <%= f.password_field :password, class:"password-textarea", id:"password", placeholder:"6文字以上の半角英数字", maxlength:"8" %>
      <p class='info-text'>※英字と数字の両方を含めて設定してください</p>
    </div>

    <div class="form-group">
      <%= f.label :password_confirmation, "パスワード(確認)", class:"label" %><br>
      <%= f.password_field :password_confirmation, class:"password-confirmation-textarea", id:"password-confirmation", placeholder:"同じパスワードを入力して下さい", maxlength:"8" %>
    </div>

    <div class="form-group">
      <%= f.label :last_name, "姓(全角)", class:"label" %><br>
      <%= f.text_area :last_name, class:"last-name-textarea", id:"first-name", placeholder:"例) 田中", maxlength:"8" %>
    </div>

    <div class="form-group">
      <%= f.label :first_name, "名(全角)", class:"label" %><br>
      <%= f.text_area :first_name, class:"first-name-textarea", id:"last-name", placeholder:"例) 角栄", maxlength:"8" %>
    </div>
    
    <div class="form-group">
      <%= f.label :last_name_kana, "姓 カナ (全角)", class:"label" %><br>
      <%= f.text_area :last_name_kana, class:"last-name-kana-textarea", id:"first-name-kana", placeholder:"例) タナカ", maxlength:"8" %>
    </div>

    <div class="form-group">
      <%= f.label :first_name_kana, "名 カナ (全角)", class:"label" %><br>
      <%= f.text_area :first_name_kana, class:"last-name-kana-textarea", id:"last-name-kana", placeholder:"例) カクエイ", maxlength:"8" %>
    </div>

    <div class="form-group">
      <div class='form-text-wrap'>
        <label class="label">生年月日</label>
      </div>
      <div class='input-birth-wrap'>
        <%= raw sprintf(
                    f.date_select(
                      :birth_date,
                      class:'select-birth',
                      id:"birth-date",
                      use_month_numbers: true,
                      prompt:'--',
                      start_year: 1930,
                      end_year: (Time.now.year - 5),
                      date_separator: '%s'),
                    "<p> 年 </p>", "<p> 月 </p>") + "<p> 日 </p>" %>
      </div>
    </div>
    <div class='register-btn'>
      <%= f.submit "ユーザー登録" ,class:"submit-btn" %>
    </div>
  </div>
  <% end %>

</div>

<%= render "shared/footer" %>
